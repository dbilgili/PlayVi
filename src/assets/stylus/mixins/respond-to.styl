@import '../miscs/variables.styl'

media_queries = {
  mobile      : "only screen and (max-width: " + ($mobile) + ")",
}

respond-to(breakpoints)
  conditions = ()
  for breakpoint in breakpoints
    push(conditions, media_queries[breakpoint])
  conditions = join(", ", conditions)
  @media conditions
    {block}